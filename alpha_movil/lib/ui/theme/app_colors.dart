import 'package:flutter/material.dart';

class AppColors {
  // Colores Synthwave Glassmorphism
  static const Color background = Color(0xFF000000); // Negro puro
  static const Color primary = Color(0xFFFF2BD6); // Rosa Neón
  static const Color secondary = Color(0xFF00F5FF); // Cian Neón
  static const Color text = Color(0xFFEAE9FF); // Blanco humo
  static const Color textMuted = Color(0xFFA3A0D6); // Muted/Grisáceo
  static const Color success = Color(0xFF20FFB5);
  static const Color danger = Color(0xFFFF4D6D);

  // Colores para glassmorphism
  static Color glassBackground = Colors.white.withOpacity(0.06);
  static Color glassBorder = Colors.white.withOpacity(0.12);
}

